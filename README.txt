
   1.) use xampp v3.2.2
   2.) $conc = mysqli_connect("localhost","1","2");
                         1 is username of 'phpmyadmin'
                         2 is password of 'phymyadmin'
                          initially username is root and no password
  3.) is differenet username of password then changes need to be made to all php file is ("$conc = mysqli_connect("localhost","1","2");"
  4.) to begin start with adminlogin.php
